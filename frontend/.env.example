# URL base de la API del backend (en producción apunta al servidor real)
VITE_API_URL=http://localhost:3000

# URL base del servidor de mapas GeoServer
VITE_GEOSERVER_URL=http://localhost:8080/geoserver

# Tenant (municipio) a usar en desarrollo local (cuando no hay subdominio disponible)
VITE_TENANT_DEV=demo
